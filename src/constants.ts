export enum FIELDS_TYPE {
  DATETIME_LOCAL = 'datetime-local',
  TEXT = 'text',
  SELECT = 'select',
  SWITCH = 'switch',
}

export enum TASK_TYPE {
  TODO = 'TODO',
  MEETING = 'MEETING',
  EVENT = 'EVENT',
  ROUTINE = 'ROUTINE',
}
